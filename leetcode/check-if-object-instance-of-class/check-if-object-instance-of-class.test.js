const solution = require('./check-if-object-instance-of-class');

describe('', () => {
    it('테스트 1', () => {
        expect(solution()).toBe();
    });
    it('테스트 2', () => {
        expect(
            solution(),
        ).toBe();
    });
});