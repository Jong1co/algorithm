const solution = require('./161990-1');

describe('', () => {
    it('테스트 1', () => {
        expect(solution()).toBe();
    });
    it('테스트 2', () => {
        expect(
            solution(),
        ).toBe();
    });
});